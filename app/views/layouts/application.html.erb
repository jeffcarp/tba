<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
  <title>The Better Announcements</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>

  <% if @checkboxes %>
    <%= javascript_include_tag "ios-checkboxes" %>
    <%= stylesheet_link_tag    "ios-checkboxes" %>
    <script type="text/javascript">
      $(window).load(function() {
        $('input:checkbox').iphoneStyle();
      });
    </script>
  <% end %>

  <%= analytics_init local: Rails.env.development? %>

  <link href='http://fonts.googleapis.com/css?family=Open+Sans:800' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,700' rel='stylesheet' type='text/css'>

  <meta name="viewport" content="initial-scale=1.0, maximum-scale=1.0" />
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <h1 style="z-index=1;position:relative;"><%= link_to 'The Better Announcements', :root %></a></h1>

    <h4 style="margin: 0 0 12px;">TBA is hibernating until Jan Plan 2013!</h4>

    <nav style="z-index=1;position:relative;">
      <% if current_user %>
        <%= render partial: 'layouts/nav' %>
      <% else %>
        <span>An alternative to the general announcements for Colby kids.</span>
      <% end %>
    </nav>
  </header>
  <div id="wrap">
    <div id="content">
      <%= yield %>
    </div>
    <footer>
      <p><strong>The Better Announcements</strong> are made by Jeff Carpenter '12, John Kalin '14, Mike Loginoff '16, and you. You can reach all of us at <a href="mailto:hello@announcements.io">hello@announcements.io</a>.</p>
    </footer>
  </div>
</body>
</html>
