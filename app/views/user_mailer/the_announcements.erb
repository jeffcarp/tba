<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
<head>
</head>
<body style="font-family: 'Helvetica-Neue', 'Helvetica', 'Arial', sans-serif;">

  <div class="header" style="margin: 0 0 40px;">
    <h1 style="font-size: 36px; color: #222; text-align: center;">The Better Announcements</h1>
    <h2 style="font-size: 14px; color: #555; text-align: center;"><%= @issue.publish_date.strftime('%B %-d, %Y') %></h2>
  </div>

  <%= render :partial => "user_mailer/post", :collection => @issue.posts %>
  
  <!-- note: add settings and compose links -->
  
    <p style="margin: 0 0 24px;">
      <% 
      if Rails.env == 'development'
        uri_prefix = 'http://localhost:3000/'
      else
        uri_prefix = 'http://shanghai.herokuapp.com/'
      end
      %>
      From this email (and all subsequent ones) you can <a href="<%= uri_prefix %>auth?e=<%= u @user.email %>&s=<%= u @user.salt %>&l=<%= u '/settings' %>">change</a> your settings or <a href="<%= uri_prefix %>auth?e=<%= u @user.email %>&s=<%= u @user.salt %>&l=<%= u '/compose' %>">send</a> an announcement.
    </p>
  
  </body>
</html>